[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "sigil"
version = "0.1.0"
description = "Sigil — give Claude full control of Obsidian"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.2.0",
]

[project.scripts]
sigil = "obsidian-mcp.__main__:main"
